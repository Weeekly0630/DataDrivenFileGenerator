### Preserved Key for DataDrivenGenerator
TEMPLATE_PATH: "var/v_ctr.xdm" # Type of the template, cannot be changed
CHILDREN_PATH: []

### User data
name: "port callback" # Name of the container
type: "" # Type of the container, e.g., "IDENTIFIABLE"
UUID: "" # UUID for the container
description: ""
variantConfig:
  - classType: "" # Class type for the variant configuration
    className:
    name:

invalidConfig:
  xpath:
    - expr: { type: function, args: [dataNode:get_rel_path, ./ctr1.yaml] }
      true_text:
      false_text:

  range:
    - expr:
      true_text:
      false_text:

optional: true
require_index: true
enableConfig:
  type:
  tsts:
    - expr:
      true_text:
      false_text:
  value:

editableConfig:
  type:
  tsts:
    - expr:
      true_text:
      false_text:
  value:

visibleConfig:
  type:
  tsts:
    - expr:
      true_text:
      false_text:
  value:

tab:
