### Preserved Key for DataDrivenGenerator
TEMPLATE_PATH: "var/v_lst.xdm" # Type of the template, cannot be changed
CHILDREN_PATH: [/*/DioEcucPartitionRef.yaml]

### User data
name: "DioEcucPartitionRef" # Name of the container
type: "" # Type of the container, e.g., "IDENTIFIABLE"
UUID: "" # UUID for the container

minConfig:
  type: 
  tsts:
    - expr:
      true_text:
      false_text:
  value:

maxConfig:
  type: 
  tsts:
    - expr:
      true_text:
      false_text:
  value:

invalidConfig:
  xpath:
    - expr: {type: function, args: [dataNode:get_abs_path]}
      true_text:
      false_text:
  # range:
  #   - expr:
  #     true:
  #     false:
editableConfig:
  type: 
  tsts:
    - expr:
      true_text:
      false_text:
  value:

visibleConfig:
  type: 
  tsts:
    - expr:
      true_text:
      false_text:
  value:

enableConfig:
  type: 
  tsts:
    - expr:
      true_text:
      false_text:
  value:

lable:

tab:

rows:
