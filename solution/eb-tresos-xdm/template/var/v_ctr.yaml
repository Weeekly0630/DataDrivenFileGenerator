### Preserved Key for DataDrivenGenerator
TEMPLATE_PATH: "var/v_ctr.xdm" # Type of the template, cannot be changed
CHILDREN_PATH: [./*.yaml]

### User data
name: # Name of the container
type: # Type of the container, e.g., "IDENTIFIABLE"
UUID: # UUID for the container
description: 

variantConfig:
  # - classType: # Class type for the variant configuration
  #   className:
  #   name:

invalidConfig:
  xpath:
    - expr:
      true_text:
      false_text:

  range:
    - expr:
      true_text:
      false_text:

optional: 
require_index: 
enableConfig:
  type:
  tsts:
    - expr:
      true_text:
      false_text:
  value:

editableConfig:
  type:
  tsts:
    - expr:
      true_text:
      false_text:
  value:

visibleConfig:
  type:
  tsts:
    - expr:
      true_text:
      false_text:
  value:

tab:
